<UserControl x:Class="Panoramas_Editor.Stub"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Panoramas_Editor"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">
    
    <Grid>
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <TextBlock Text="Для начала работы выберите изображение"
                       FontSize="20"
                       Foreground="{StaticResource IconsBrush}"/>

            <fa:ImageAwesome Icon="PictureOutline"
                             Height="100"
                             Foreground="{StaticResource IconsBrush}"
                             Margin="20"/>

        </StackPanel>
    </Grid>
</UserControl>
